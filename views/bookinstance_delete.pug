extends layout

block content
  h1 Book Instance: #{bookinstance._id}


  p Do you really want to delete this Book Instance?

    form(method='POST' action='')
      div.form-group
        // the body of this POST request will be 
        // (name : value) = ('bookinstanceid' : bookinstance._id)
        input#bookinstanceid.form-control(type='hidden',name='bookinstanceid', required='true', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete

